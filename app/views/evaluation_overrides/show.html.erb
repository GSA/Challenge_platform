<h1>Submission ID <%= @submission.id %></h1>
<p class="font-body-sm text-normal margin-bottom-3">Review evaluator's scores and provide your revisions</p>

<h2 class="text-primary font-body-md"><%= "Evaluator: #{@evaluator.full_name}" %></h2>

<%= render partial: "evaluation_overrides/form" %>