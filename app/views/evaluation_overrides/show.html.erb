<%= render 'shared/back_link', path: @return_path %>

<h1>Submission ID <%= @submission.id %></h1>
<p class="font-body-sm text-normal margin-bottom-4">Review evaluator's scores and provide your revisions</p>

<h2 class="text-primary text-no-underline text-bold font-sans-lg"><%= "Evaluator: #{@evaluator.full_name}" %></h2>

<%= render partial: "evaluation_overrides/form" %>