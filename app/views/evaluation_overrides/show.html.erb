<h1><%= challenge_phase_title(@submission.challenge, @submission.phase) %></h1>
<h2 class="text-primary">
  <%= "#{@evaluator.full_name}'s Evaluation for Submission ID #{@submission.id}" %>
</h2>

<%= render partial: "evaluation_overrides/form" %>